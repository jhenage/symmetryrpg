import { TimeButtonDirective } from './time-button.directive';

describe('TimeButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new TimeButtonDirective();
    expect(directive).toBeTruthy();
  });
});
